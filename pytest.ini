[pytest]
# O projeto mistura dois estilos de import (`core...` e `backend.core...`),
# herança das trocas de ferramenta. Ate padronizar (item 5 do TODO), colocamos
# tanto a raiz quanto backend/ no sys.path para os dois estilos resolverem.
pythonpath = .
testpaths = backend/tests
