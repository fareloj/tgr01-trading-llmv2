{
  "name": "tgr01-ops-console",
  "version": "0.1.0",
  "private": true,
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "concurrently -k \"vite --host 127.0.0.1\" \"wait-on tcp:5173 && electron .\"",
    "build": "vite build",
    "test": "node --test electron/*.test.cjs src/*.test.mjs",
    "test:electron": "npm run build && electron electron/smoke.cjs",
    "start": "electron .",
    "repair-electron": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/repair-electron.ps1"
  },
  "dependencies": {
    "lucide-react": "^0.511.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "concurrently": "^9.1.2",
    "electron": "42.3.0",
    "vite": "^6.3.5",
    "wait-on": "^8.0.3"
  }
}
